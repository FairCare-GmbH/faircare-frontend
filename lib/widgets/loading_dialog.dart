import 'package:ars_dialog/ars_dialog.dart';
import 'package:flutter/material.dart';

CustomProgressDialog loadingDialog(BuildContext context) {
  CustomProgressDialog dialog = CustomProgressDialog(context, blur: 0);
  return dialog;
}
